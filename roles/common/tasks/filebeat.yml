- name: clone infra ansible repo
  ansible.builtin.git:
    repo: https://github.com/devilathag/Infra-Ansible1.git
    dest: /tmp/infra-ansible

- name: run shell command
  shell: pwd
  register: out

- name: debug
  debug:
    msg: "{{ out }}"